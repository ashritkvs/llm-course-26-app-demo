# Copy to .env and fill in your values

# Optional: Groq API key. If set, Groq llama3.2-90b is used instead of local Ollama.
GROQ_API_KEY=

# Optional: GitHub personal access token (read-only scopes are enough).
GITHUB_TOKEN=
